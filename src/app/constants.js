// src/app/constants.js
// Zentrale Konstanten, die von mehreren Modulen genutzt werden.

export const CONTROL_ACTIONS = Object.freeze({
  pause: "pause",
  resume: "resume",
  skip: "skip",
  queue: "queue",
  stop: "stop",
});
